# Post-merge audit — `0c477e1..b0504ae`

Date: 2026-09-16
Auditor: post-merge audit agent (Opus 5)
Range: `0c477e16f35c76002b21f1463203115a0c930da7..b0504aee81d3e96f58af2ebfd6f254a1cb2b2e6a`

## Commits reviewed

| SHA | Subject |
|-----|---------|
| `007dc53` | roadmap: task 60 -> in_progress |
| `d628432` | harness: agent delivery — task 60 (run `run-1789552824836-21fbde8e`) |
| `b0504ae` | roadmap: task 60 -> done (shipped `d6284325a7ac`) |

Code surface in the range is a single file: `test/delta_calc/manifest_consistency_test.exs`
(+33 / −12). The other two commits are roadmap bookkeeping.

## What landed

Task 60 removed the hand-maintained module set from the manifest gate:

- `documented_modules_from_lib/0` now walks `lib/**/*.ex` via `Path.wildcard/1` instead of a
  non-recursive `File.ls!` on `lib/delta_calc/`, so invariant 5 (publicly documented module must
  be registered) sees top-level `lib/*.ex` and any future subdirectory.
- Invariant 6's doctest set became `documented_modules_from_lib() ++ Manifest.modules()`,
  replacing task 59's literal `[DeltaCalc, Manifest | Manifest.modules()]`.
- Invariant 5 keeps its two deliberate exemptions, but now by module identity
  (`&(&1 in [DeltaCalc, Manifest] or ...)`) rather than by rejecting the `manifest.ex` filename —
  the right axis, since the widened scan also picks up `lib/delta_calc.ex`.
- `module_from_file/1` now takes a full path (it previously joined `@lib_delta_calc` itself),
  which is what let both scans share it.
- A `@tag :tmp_dir` test feeds a synthetic path list (nested + top-level + manifest) through
  `documented_modules_from_paths/1` and asserts the derived module set, plus the empty-list case.

Assessment: the delivery meets all five acceptance criteria, stays inside its declared write-set
(`test/delta_calc/manifest_consistency_test.exs`), changes no `lib/` behavior, and leaves no dead
code, debug output, or stray markers. Renaming the two affected test descriptions to match their
widened scope was a nice touch the criteria did not require.

No reviewer rejections are recorded for this project, so there is no false-rejection call to make.

## Findings

Two findings, one class each.

| # | File | Finding | Severity |
|---|------|---------|----------|
| 1 | `test/delta_calc/manifest_consistency_test.exs:293` | `api_modules_from_lib/0` — the scan behind invariant 2 — was left on the old non-recursive `File.ls!("lib/delta_calc")`. Task 60's acceptance criteria named only `documented_modules_from_lib/0`, so this was in-spec, but it leaves the gate half-derived: the exact hole task 60 exists to close still stood for invariant 2. | minor |
| 2 | `lib/delta_calc/manifest.ex:11,14,18` · `CLAUDE.md:102` (→ `AGENTS.md`) · `CHANGELOG.md:10` | All three prose statements of the invariants still described the pre-task-60 shape: invariant 2 and 5 scoped to `lib/delta_calc/`, and invariant 6 as "the registered set plus `DeltaCalc` and `DeltaCalc.Manifest`" — the literal carve-out task 60 deleted. Same documented-output-drift class the previous audit (`0c477e1`) fixed for task 59, recurring because task 60's write-set was again a single test file. | minor |

On finding 1: an api()-bearing module in a `lib/` subdirectory would have been invisible to
invariant 2. Invariant 5 caught it transitively *if* it was publicly documented, so the practical
exposure was an api()-bearing module carrying `@moduledoc false` in a subdirectory — narrow, and
`lib/` is flat today. Still the last hand-scoped path walk in the file, and the residual is what
would have made a "third instance" task fileable, which task 60's own body argues against.

## Fixes applied

1. **`test/delta_calc/manifest_consistency_test.exs`** — extracted `lib_sources/0`
   (`Path.wildcard("lib/**/*.ex")`) and pointed both `api_modules_from_lib/0` and
   `documented_modules_from_lib/0` at it. Dropped the now-unused `@lib_delta_calc` attribute and
   the redundant `manifest.ex` filename reject (`DeltaCalc.Manifest` exports no `__api__/0`, and
   invariant 5 already exempts it by module). Renamed the invariant-2 test to
   "every lib module with api() functions is registered in Manifest", and restated the test
   moduledoc's scope line.

   Verified by construction, not by assumption: a temporary
   `lib/delta_calc/nested/probe.ex` with a real `api()` annotation was added, and **both**
   invariant 2 and invariant 5 failed naming `DeltaCalc.Nested.Probe` (before this fix only
   invariant 5 would have). The probe was removed and the suite is green again.

2. **`lib/delta_calc/manifest.ex`** — invariants 2, 5 and 6 restated for the derived set, with a
   closing line noting all three walk `lib/**/*.ex` recursively. This moduledoc ships in the hex
   package, so it is the consumer-facing statement of the rule.

3. **`CLAUDE.md`** (and `AGENTS.md`, regenerated with `sync-agents-md.sh`; `--check` clean) —
   same restatement in the § "Review Blind Spots" paragraph. This is the text the cross-family
   reviewer reads, so leaving it stale risks a future diff being graded against the superseded
   rule.

4. **`CHANGELOG.md`** — the Unreleased invariant-6 bullet now describes the derived scan instead
   of the carve-out list, and records that the registration invariant shares it. The entry is
   still unreleased, so it is the release-note text for tasks 58, 59 and 60 together; the 0.3.0
   section's own reference to `lib/delta_calc/` was left alone as released history.

## Tasks filed

None. The one residual worth acting on (finding 1) was fixed forward here rather than filed —
it is three lines inside the same file task 60 owned, and filing it would have been the third
instance of a class whose class-statement task has already landed. The remaining known
limitation — `module_from_file/1` reads only the first `defmodule` per file — is speculative
against a one-module-per-file tree and is not worth a task.

## Checks

`mix check.dispatch` (format --check-formatted, compile --warnings-as-errors, test, credo
--strict, ex_dna --max-clones 0) — green, cold, after `mix deps.get` in this un-warmed worktree:
559 tests passing (2 doctests, 31 properties), credo clean over 58 files, 0 clones. Per `mix.exs`,
this alias deliberately omits dialyzer and `reach.check`; the changes here are a test file and
four docs, so neither adds signal that the landed-base `mix ci` will not.
