# Post-merge audit — `3b3e4d5..9732c4d`

**Date:** 2026-09-16
**Range:** 3 commits (`3b3e4d5` .. `9732c4d`), task 56 — *Assert the two fully-elided README
scalar results instead of hiding 34-digit precision*
**Delivered by:** codex (`gpt-6-astra`) · run `run-1789538428174-b2afded6`

## What was reviewed

| Commit | Subject |
|---|---|
| `3b3e4d5` | roadmap: task 56 -> in_progress |
| `640998d` | harness: agent delivery — task 56 (`README.md`, `test/delta_calc/readme_examples_test.exs`) |
| `9732c4d` | roadmap: task 56 -> done |

Net product diff over the range: **7 lines**. Two README examples (`Pnl.breakeven/1` ~line 411,
`Fees.funding_adjusted_breakeven/3` ~line 576) gained a `|> Decimal.round(2)` output boundary plus a
one-line explanatory comment and a concrete `#=> #Decimal<50030.01>`; the README-example census in
`readme_examples_test.exs` moved `{36, 16}` -> `{38, 14}`.

Reviewed for: acceptance-criteria coverage, value correctness computed independently of the code,
census integrity, residual elisions, dead code, debug leftovers, CHANGELOG / CLAUDE.md / AGENTS.md
doc coverage, roadmap render drift, and project conventions.

## Verdict on the landed work

**Sound.** All five acceptance criteria are met and the judgement call the task named (round in the
README example vs. document the 34-digit literal vs. round inside the function) was resolved the
right way: the rounding is on the caller side of the example, `lib/` is untouched, and the added
comment states *why* — which makes the example a better front-door demonstration of the v0_3
caller-controlled-rounding contract than the elided version it replaced.

Verified independently of the code, per the repo's golden-provenance invariant. Long breakeven
with `entry = 50000`, `size = 2`, `open = 0.0004`, `close = 0.0002`:

```
(B - 50000) x 2 = 50000 x 2 x 0.0004 + B x 2 x 0.0002
2B - 100000     = 40 + 0.0004B
1.9996B         = 100040
B               = 50030.006001...  ->  Decimal.round(2) = 50030.01
```

`Fees.funding_adjusted_breakeven/3` with `accrued_funding = 0` reduces to the same expression, so
the identical documented result is correct rather than copy-paste. Both match the value the task
body predicted.

Census re-derived independently by re-implementing the extractor against the current README:
**38 asserted, 14 elided, 52 total** — matches the pinned assertion. All 14 remaining elisions were
enumerated and every one is a map/list **tail** elision (`..., ...}`, `[...]`, `%{k: ...}`), so
AC 3 ("the elision rule stays as-is for map/list tail elisions — only whole-result scalar elisions
are removed") holds exactly. No `#=> #Decimal<...>` whole-result elision remains.

No reviewer rejections are recorded for this project, so nothing to re-adjudicate.

## Findings

### 1. CHANGELOG gap — **fixed** (minor)

Task 56 changed two documented return values on the library's front door and left no CHANGELOG
trace. The `Unreleased` section carried bullets for task 54 (liquidation formula) and task 55 (the
README-example gate) but nothing for 56, so a consumer reading the changelog would see that README
examples are now executed without learning that two of them changed shape.

Folded into the existing README-example bullet rather than opened as a new one — it is the same
story (the doc-rot gate and its coverage), and a separate release-level bullet for a two-example
documentation change would be noise in a file that states it holds "release-level history".

### 2. The elision rule is all-or-nothing — 44 documented values go unchecked — **filed as task 57** (moderate)

Verifying AC 3 surfaced a structural gap that task 56 did not create and explicitly excluded from
its scope (`out_of_scope`: *"Changing the README-example test's extraction or comparison mechanics"*,
*"Auditing map/list tail elisions elsewhere in the README"*), so this is not a defect in the landed
work — but it is the larger remaining share of the same hole.

`elided?/1` is `String.contains?(example.documented, "...")`
(`test/delta_calc/readme_examples_test.exs:156`), so a **single** `...` tail opts out the **entire**
example. Measured against the current README, the 14 elided examples document **57** key/value
pairs of which only **13 positions are actually elided** — the other **44 are concrete and
unchecked**:

```
final_avg_entry: #Decimal<2910.638297872340425531914893617022>
liquidation_price: #Decimal<1507.537688442211055276381909547739>
daily_funding_rate: #Decimal<0.045>,  periods_per_day: #Decimal<3>
total_exposure: #Decimal<124500>,     portfolio_margin: #Decimal<27.0000>   ...
```

Tasks 55 and 56 together closed the gate on **3** whole-result scalars; these 44 remain, and they
sit in precisely the multi-field decision maps (`DCAPlanner`, `MarginBridge`, `StressScenario`,
`PortfolioMargin`, `DeltaNeutral`) that phase-3 behavior changes move — the same maps the
2026-09-16 README audit found had already rotted before task 55 landed.

Filed as **task 57** — *"Assert inside tail-elided README examples so no elision can hide a concrete
value"* (phase 3, `contract_hardening`, d4/b5/u4, `codex` / `gpt-6-astra`). Scoped at the **class**
level rather than as another instance: the fix is a subset comparison in the extractor (compare
every documented position that is not `...`), after which no elision can hide a concrete value
again. Sibling check against 55/56 recorded in the task's leading comment.

### 3. Three asserted results are 34 digits long — **not fixed, folded into task 57** (nitpick)

`README.md:68` (`Calc.liquidation` — the *first* example a `Calc` reader sees), `:510`
(`PortfolioMargin.portfolio_liquidation_price`) and `:602` (`Carry.breakeven_funding`,
`#Decimal<-0.0001111111111111111111111111111111111>`) document full context precision. These are
**not** a correctness gap — the gate asserts them — but they are the same illegibility argument
task 56 made, and they leave the README carrying two conventions for the same situation. Not
corrected here because choosing a convention is a judgement call across three modules, not a
hygiene fix; carried as the secondary item of task 57 ("pick one convention and apply it
consistently").

## Checks not flagged

- **AGENTS.md freshness** — `sync-agents-md.sh --check` reports up to date (CLAUDE.md untouched in
  this range).
- **Roadmap render drift** — `rmap render` produced a byte-identical `ROADMAP.md` / `data.json`
  before my own task-57 filing.
- **Dead code / debug output / naming** — none; the product diff is 7 lines of Markdown plus one
  integer pair.
- **`lib/` behavior** — untouched in the range, as AC 4 required.

## Cold-build witness

Ran in this intentionally un-warmed worktree (no copied `deps`, `_build` or PLTs):

```
mix deps.get && mix check.dispatch
```

**Green** — 548 tests pass (31 properties, 517 tests), credo `--strict` finds no issues across 58
files, `ex_dna` reports 0 clones against a 0 budget. The compile warnings in the output all
originate in dependency source (`dialyzer_json`, `doctor`, `vibe_kit`), none in `delta_calc`.

## Fixes applied in this audit commit

| File | Change |
|---|---|
| `CHANGELOG.md` | Extended the README-example bullet with task 56's outcome (finding 1) |
| `roadmap/tasks.toml`, `ROADMAP.md`, `roadmap/data.json` | Filed task 57 (finding 2) |
| `.audit/9732c4d.md` | This report |
