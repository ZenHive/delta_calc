# Post-merge audit — `b0504ae..ed3220a`

Date: 2026-09-20
Auditor: post-merge audit agent (Cursor Grok 4.6)
Range: `b0504aee81d3e96f58af2ebfd6f254a1cb2b2e6a..ed3220ac0bcdcdc4f96d92e570e191437f521aa1`

## Commits reviewed

| SHA | Subject |
|-----|---------|
| `9f5d379` | audit(b0504ae): finish task 60's derivation — invariant 2 walks lib/ too, and restate the gate in four docs |
| `ed3220a` | docs: centralize verification scope and defer full QA to audit |

`9f5d379` is itself the previous post-merge audit (test helper + four docs). `ed3220a` is
docs-only: `CLAUDE.md` imports `verification-policy.md`; `AGENTS.md` is the generated expansion.

## What landed

Task 60's residual (invariant 2 still walking `lib/delta_calc/` non-recursively; invariants 2/5/6
stated in the old shape) was already fixed forward in `9f5d379`. Re-read at this tip:

- `lib_sources/0` (`Path.wildcard("lib/**/*.ex")`) feeds both `api_modules_from_lib/0` and
  `documented_modules_from_lib/0`.
- `DeltaCalc.Manifest` moduledoc, `CLAUDE.md` § Review Blind Spots, and the Unreleased CHANGELOG
  bullet all describe the derived recursive scan.
- No dead code, debug output, or leftover markers. The `@lib_delta_calc` attribute is gone.

`ed3220a` is the intended policy move: implementer/reviewer stay on focused checks;
full QA is this audit seat. No library behavior change. CHANGELOG is correctly silent — this is
not a consumer-facing API note.

No reviewer rejections are recorded for this project, so there is no false-rejection call.

## Findings

Three, all hygiene around the policy import.

| # | File | Finding | Severity |
|---|------|---------|----------|
| 1 | `AGENTS.md` | `sync-agents-md.sh --check` red at the integrated SHA. Drift is host-side transitive includes (`harness-workflow.md` gained Integrated audit and QA / implementer question channel; this node's `elixir-setup.md` still has the four-tier alias template that `ed3220a` had inlined from a newer host copy). Same class as audit `c39365c`. | minor |
| 2 | `mix.exs` `ci` alias | First step was `format` (rewriter), then `format --check-formatted`. The check could not fail. The newly imported elixir-setup text names this exact gate bug. Pre-existing, made salient by this range. | minor |
| 3 | `CLAUDE.md` reviewer note | Pointed at `AGENTS.md` § Toolchain & check commands, a heading that did not exist. `ed3220a` edited that sentence and kept the dead pointer. | minor |

`9f5d379` itself is clean. Reach still prints two pre-existing non-failing smells
(`presets.ex:41` duplicate `Decimal.new`, four repeated map shapes in `option_ladder.ex`); they
predate this range and were already declined as tasks.

## Fixes applied

1. **`mix.exs`** — `ci` now starts with `format --check-formatted` only. Dropped the rewriter
   and the duplicate check after compile. Comment on `check.dispatch` now says post-merge
   audit + QA runs `mix ci`.
2. **`CLAUDE.md`** — added `## Toolchain & check commands` listing this repo's actual aliases
   (`check.dispatch` includes test/credo/ex_dna; `mix ci` does not include doctor/sobelow/
   coverage). Reviewer note points here. Quality Gate kept as the coverage-tier pointer.
3. **`AGENTS.md`** — regenerated with `sync-agents-md.sh`. `--check` clean. Regenerating also
   picked up current host includes (Integrated audit and QA, question channel). The inlined
   elixir-setup alias snippet reverted to this node's four-tier template; the new Toolchain
   section is the project-specific authority and says so.

Verified after the edits: `sync-agents-md.sh --check` OK, `mix format --check-formatted mix.exs`
OK, `mix compile --warnings-as-errors` OK. Full QA below was run at the integrated SHA *before*
these edits, per the audit contract.

## Tasks filed

None. The three findings were inline-doable. No leftover class that needs a roadmap row:
aligning `mix.exs` to the generic elixir-setup template would *weaken* `check.dispatch` (it
would drop the test/credo/ex_dna steps this project already runs). Hex advisories on
dev-only HTTP deps (bandit / mint / igniter, flagged at `mix deps.get`) are published, not
in the hex package file set, and are not a failed check.

## Full-project QA

Cold worktree. `.tool-versions` pins elixir 1.19.1-otp-28 / erlang 28.4, neither installed on
this node. Ran on installed Elixir 1.20.4 / OTP 29, which satisfies `mix.exs` `elixir: "~> 1.15"`.
`mix deps.get` then the configured command, continuing after each step:

| Step | Exit | Outcome |
|------|------|---------|
| `mix ci` | 0 | format rewriter did **not** mutate the tree; 559 passed (2 doctests, 31 properties); credo `--strict` clean (58 files); dialyzer 0 errors (cold PLT built); `ex_dna --max-clones 0` clean; reach `--arch` OK, 2 non-failing smells |
| `mix doctor --raise` | 0 | 26/26 modules, 100% doc / moduledoc / spec |
| `mix sobelow --skip --exit Low` | 0 | "cannot find the router" (expected; no Phoenix); scan complete; `.sobelow-skips` still covers the pre-existing OptionsRisk BinToAtom skip |
| `mix test.json --cover` | 0 | 559 passed, 0 failed; coverage 1068/1073 lines (99.53%); `DeltaCalc.Calc` 100%; `DeltaCalc.Hedging` 98.84% (line 622 uncovered) — both at/above the 95% money-math tier |

**QA status: passed.** Log: `/tmp/delta-calc-qa-qNGv94/full.log`.

## Reviewer-quality note

No reviewer rejections recorded for this project. Neither commit in the range is a harness
task delivery, so there is no verdict to cross-check.
