# Audit — a079c23 (range abe988f..a079c23)

**Scope:** post-merge hygiene pass over the 3 commits that landed task 35
(`Fix Funding.compare_funding_rates cross-cadence ranking`).

```
a079c23 roadmap: task 35 -> done (shipped 241ee793845f)
241ee79 harness: agent delivery — task 35 (run run-1781736368569-01d47010)
7b043ce roadmap: task 35 -> in_progress
```

Code touched: `lib/delta_calc/funding.ex` (+65), `test/delta_calc/funding_test.exs` (+65),
plus roadmap status churn (`tasks.toml`, `data.json`).

## Verdict: clean — no fix-forward edits

The diff adds a `%{venue => periods}` cadence branch to `compare_single_symbol/2` so that
mixed-cadence venues are ranked by per-day-normalized rate, with the arbitrage threshold
normalized to daily terms (`0.0005 * 3 = 0.0015`, hoisted as `@mixed_cadence_arbitrage_threshold`).

### Hygiene checks
- **Dead code:** none. New helpers (`daily_rates/2`, `annual_apr_delta/1`, `rank_venues/2`) are all reachable from the new map clause.
- **Debug leftovers:** none (`IO.inspect` / `dbg` grep clean).
- **Docs:** `@doc` updated to document mixed-cadence semantics and the daily-normalized threshold; the `api()` annotation already covers the scalar-or-map `periods_per_day` shape. `@spec`s present on all new privates.
- **Naming / conventions:** consistent with the module. Minor readability note (not fixed): the map clause binds a local `daily_rates` from the function `daily_rates/2` of the same name — Elixir resolves this unambiguously, and churning reviewer-approved code for a nitpick isn't warranted.
- **CHANGELOG:** project keeps none — no gap.
- **README:** `README.md:218` still shows only the simplest single-symbol call, which remains accurate; the mixed-cadence form is an advanced path documented in the `@doc`. Left as-is (surfacing it in README is scope creep for an audit).

### Acceptance criteria (task 35) — all satisfied by the landed code
1. Mixed-cadence never returns negative `annual_apr_delta` — `delta = max_daily − min_daily ≥ 0`, annualized as `delta·365·100`. ✓
2. `max_exchange`/`min_exchange` reflect highest/lowest annualized rate — ranked by daily rate, monotonic with annualized. ✓
3. Scalar-cadence output byte-identical — the scalar clause is untouched; test `preserves scalar cadence comparison output` pins it. ✓
4. Threshold semantics documented in `@doc`. ✓
5. Regression test for the hourly-vs-8h ranking inversion present. ✓

Checks not re-run: the cross-family reviewer already gated this work and it landed; no
fix-forward code edits were made here, so re-running `mix ci` (cold-PLT dialyzer in a fresh
worktree) would re-grade already-graded code for zero signal.

## Discovery filed
- **task 36** — *Reconcile Funding `:delta` units across scalar vs venue-map cadence paths*.
  Whole-surface domain observation that per-task review cannot see: `:delta` now carries a
  different unit depending on the `periods_per_day` argument shape (raw per-period for a scalar,
  per-day-normalized for a venue map). This is documented and intentional, but it leaks into
  `find_arbitrage_opportunities/2`, whose default `min_delta` (`0.001`) was tuned for the raw
  per-period scale — so genuine daily-normalized spreads can be filtered out, and scalar- vs
  map-derived comparison results cannot be safely mixed in one call. Filed for a domain decision,
  not fixed forward (intentional, documented behavior; the resolution is a contract choice).

## Reviewer false-rejection check
No reviewer rejections recorded for this project — nothing to reconcile.
