---
sha: 963551131ada753232564f595647f25c7a71aa75
short_sha: 9635511
audited_at: 2026-06-17
auditor_model: claude-opus-4-8
verdict: findings-accepted
codex_status: dual-reviewer
audited_by: audit-review v1
---

# Audit: task 30 — OptionsRisk thread periods_per_day + rename total_90d

**Original commit:** `9635511` (+ `roadmap: task 30 -> done` status flip).
**Files touched:** lib/delta_calc/options_risk.ex, test/delta_calc/options_risk_test.exs

## What the commit does (correct)

Threads `periods_per_day` (default 3, overridable to 24) into MarginBridge via
`calculate_negative_funding_impact` and `stress_test_extended_negative`, and renames the static
`total_90d` field to a dynamic `:"total_#{duration_days}d"` key. Daily-cost math verified: rate
-0.03% / 60000 / 24 periods → 432 (8× the 54 at 3/day); rate -0.02% → daily 288, total_90d 25920.
Addresses the cadence blind spot for the OptionsRisk surface.

## Finding — dynamic-atom key (Codex, pri 6) → ACCEPTED, already triaged

Codex re-flagged `:"total_#{duration_days}d"` as a caller-driven atom that could exhaust the atom
table for repeated arbitrary durations (BinToAtom).

**Resolution: accepted as a false positive, no action.** The team already triaged this in commit
`cd172e1` ("chore: skip sobelow BinToAtom FP in OptionsRisk duration-keyed result"). `duration_days`
is an operator/caller-supplied bounded `:value` in a headless calc library (no untrusted web input
crosses this boundary), so atom-table growth is not a realistic risk. Per the repo's sobelow
guidance, the `.sobelow-skips` entry IS the documented disposition. Residual note for consumers: an
app that forwards genuinely-unbounded user input as `duration_days` should clamp it upstream.

Also corrected the per-row return-doc to `total_{duration_days}d` (no doc drift remaining).

## Codex second-opinion

Status: dual-reviewer (job task-mqi6uyzg-5f75qs). Codex's finding is real in the abstract but
already resolved by the existing sobelow skip; recorded here to close the loop rather than
re-litigate.

## Auto-applied fixes

None — finding previously accepted via `cd172e1`.
